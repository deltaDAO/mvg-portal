export interface ResourceCard {
  id: string
  category: string
  tag: string
  title: string
  description: string
  image: string
  link: string
}

export interface Tab {
  id: string
  label: string
}
