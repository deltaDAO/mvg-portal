import React, { ReactElement } from 'react'
import styles from './Main.module.css'

export default function Main({
  currentStep = 0,
  steps
}: {
  currentStep: number
  steps: ReactElement[]
}): ReactElement {
  return <div />
}
